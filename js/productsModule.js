'use strict';

angular.module('productsFilter', []);

.directive('ngDropdownMultiselect', ['$filter', '$document',
    function ($filter, $document) {

        return {
            restrict: 'AE',
            scope: {
                
            },
            templateUrl: '',
            link: function ($scope, $element, $attrs) {
                
            }
        };
}]);
